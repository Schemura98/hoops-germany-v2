import { Inter } from "next/font/google";

// Globale Schriftart laut Design-System (Abschnitt 9 der Spezifikation)
export const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});
