# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/.next-dev/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# user uploads (auf VPS persistent, nicht in git)
/public/team/
/public/players/
/public/posts/

# Wegwerf-Skripte, Screenshots und Logs aus Prüf-/Gate-Läufen (Agenten-Arbeitsreste)
/tmp/
# dito – die Sitzungs-Ablage. ⚠️ Sie war bis zum 20.08.2026 NICHT ignoriert, und darin
# lagen drei Sicherungskopien echter Quelldateien (`*.orig`). Genau so eine Kopie hat am
# 18.08.2026 `app/kontakt/page.js` überschrieben, ohne dass Build oder 253 grüne Tests es
# gemerkt haben. Eine Kopie einer Quelldatei im Projektordner ist eine Falle, ob sie nun
# eingecheckt wird oder nicht – ignoriert wird sie wenigstens nicht versehentlich Teil
# eines `git add -A`.
/scratchpad/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
tests/e2e/.artifacts/
