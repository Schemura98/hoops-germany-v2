export const metadata = { title: "Newsfeed – Hoops Germany" };

export default function HomePage() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center p-8 text-center">
      <h1 className="text-2xl font-bold text-gray-900">Newsfeed</h1>
      <p className="mt-2 text-gray-500">
        Platzhalter – Route <code className="text-brand-600">/home</code> wird in der
        Umsetzungsphase implementiert.
      </p>
    </main>
  );
}
